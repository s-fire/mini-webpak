
(
  function(map){
    function require(filepath){
      const module = {
        exports:{}
      }
      const fn = map[filepath]
      fn(require,module)
      return module.exports
    }
    require('./example/main.js')
  }
)({
   <% graph.forEach(item=>{ %>
     "<%- item['filePath'] %>" : function(require,module){
       <%- item['code'] %>
       },
    <% }) %>
  });